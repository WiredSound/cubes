#pragma once

#include <vector>

namespace text {
    class TextMesh {
    public:
        //TextMesh(const std::vector<float>& vertices, unsigned int vertex_count);
        //void bind_and_draw();
    };
}
